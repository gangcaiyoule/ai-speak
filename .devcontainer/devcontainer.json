{
  "name": "ai-speak-flutter",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",
  "features": {
    "ghcr.io/devcontainers/features/go:1": {
      "version": "1.22"
    }
  },
  "remoteEnv": {
    "PATH": "/opt/flutter/bin:${containerEnv:PATH}"
  },
  "onCreateCommand": "echo 'skip auto flutter install; run bash .devcontainer/setup-flutter.sh manually after boot'",
  "postCreateCommand": "echo 'codespace ready; run: bash .devcontainer/setup-flutter.sh && cd mobile && flutter pub get'",
  "customizations": {
    "vscode": {
      "extensions": [
        "Dart-Code.dart-code",
        "Dart-Code.flutter"
      ]
    }
  }
}
