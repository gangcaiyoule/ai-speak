# D5 日报

## 日期

2026-09-05

## 昨日遗留问题的回答

1. **提交身份核对。** 昨日 `feat/coaching` 分支的开发提交 `c4850f7` 作者为 `yejunbo`，邮箱为 `692979649@qq.com`。今日 `feature/IKD13O-auth` 分支上的非合并开发提交为 `25828463`，作者和邮箱同样是 `yejunbo <692979649@qq.com>`；`e25151f` 属于合并提交，不能作为功能开发归属证明。`eba120b` 在当前本地对象中无法解析，未据此推断作者。若由 Codex 代理执行提交，代理只是代执行 Git 命令，提交身份读取本机 Git 配置；本次机械核对读到的就是本人配置的 `yejunbo / 692979649@qq.com`。今天新产生的 `99d17cb` 和 `43e19e4` 也已核对为该作者和邮箱，作为下一笔开发提交的归属自证；但 Git 提交元数据仍未直接写入学号 `24320117`。
2. **PR 关联 Issue。** PR !3 当前关联 `IKCTD5`，PR !18 当前关联 `IKD13O`，平台页面均已显示实际绑定，不是 `#待填写`。此前文案留空是因为起草时 Issue 编号尚未最终确认，不能先编造编号；以后必须在创建 PR 前完成绑定并回读核验。
3. **R5 建议落实情况。** 认证服务端测试路径为 `server/internal/identity/http_test.go`、`server/internal/identity/service_test.go`，登录/注册页面内嵌于 `mobile/lib/identity/auth_gate.dart`，客户端位于 `mobile/lib/identity/identity_client.dart`。本日已完成 Git 身份统一，并用 `99d17cb`、`43e19e4` 两笔开发提交自证机械归属；`prompts/24320117/D5/` 仍只有 `.gitkeep`，真实认证修复或 CI 排错求助尚未导出，这项承诺尚未完成。
4. **学号对应关系。** 已确认本人是叶俊博，学号为 `24320117`；此前 README 将叶俊博和裘之语的学号对应写反。经核对后，README 已修正为：叶俊博（24320117）、裘之语（24320114）。

## 今日目标

1. 将剩余练习功能按 Ticket 独立提交、独立推送。
2. 完成练习会话生命周期和文本回答提交的 Go、Flutter、接口契约及迁移改动。
3. 核对提交身份、分支依赖和日报追踪信息，避免把未合并内容写成已合并。

## 实际进展

1. **Ticket 2：练习会话生命周期。** 从最新 `origin/main` 建立干净分支 `feature/R6-practice-session`，移植练习会话创建、问题快照、激活、当前问题查询和完成状态流转，生成并推送 `99d17cb feat(coaching): implement practice session lifecycle`。
2. **Ticket 3：文本回答提交。** 以 Ticket 2 分支为前置建立 `feature/R7-text-answers`，移植文本回答提交、重复回答和当前问题校验、问题推进、Flutter 练习进行中页面及 `0008_practice_turns.sql`，生成并推送 `43e19e4 feat(coaching): submit practice text answers`。
3. 两个分支已分别推送远程，Ticket 3 明确依赖 Ticket 2。Ticket 2 对应 Issue `IKDHFR`、PR `!32`，于 2026-09-05 16:02:48（北京时间）合并；Ticket 3 对应 Issue `IKDHFX`、PR `!33`，于 2026-09-05 19:55:46（北京时间）合并。两个 PR 的“关联 Issue”均已实际绑定。
4. 在 `server` 模块执行并通过：`go test ./...`、`go vet ./...`、`go build ./...`、`git diff --check`。Flutter/Dart SDK 仍未安装，未执行 Flutter 格式化、静态分析和测试。

## 遇到的问题与解决

1. **分支包含无关主干历史。** 原开发工作树混入主仓库后续文档、PPT 和语音资源变更，直接推送会使 PR 出现大量无关删除。解决方式是从最新 `origin/main` 创建干净 worktree，只移植 Ticket 2 和 Ticket 3 的功能提交。
2. **Ticket 3 的 `todos.md` 冲突。** Ticket 2 和 Ticket 3 都改动需求勾选，且主干已有 R8 细分记录。解决时保留主干 R8 内容，只合并 R7 的变更，避免覆盖其他成员的语音任务记录。
3. **验证命令路径问题。** 根目录执行 `go test ./...` 会因为 Go 模块在 `server` 子目录而失败；改在 `server` 目录执行后全部通过。这个失败是命令路径不适用，不是代码测试失败。
4. **过程记录仍有缺口。** 今天完成了 Git 身份统一、开发提交自证、Ticket 2/3 的 Issue/PR 创建与合并，以及 README 学号映射修正；但尚未补写 `prompts/24320117/D5/` 的真实认证/CI 求助导出，后续需要单独补齐并在日报中引用。

## 明日计划

1. 在日报中记录 README 学号映射修正对应的实际提交号。
2. 为已合并的 R6/R7 在 `todos.md` 中补充 PR 编号和真实合并时间，确保“已合并”状态可回验。
3. 将真实的认证修复、CI 排错等求助导出到 `prompts/24320117/D6/`，并在日报引用具体文件。
4. 基于已合并的会话和文本回答功能，继续检查 Flutter 端联调与后续 R8 语音边界。
