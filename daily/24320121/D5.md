# D5 日报

## 昨日遗留问题的回答

1. **追问①（映射实现与 START_DATE）**：双路径映射不是软链、不是重复文件、也不是脚本改动，而是纯文档约定——`3d8ad0f` 在 `daily/24320121/D3.md` 落地「课程序号↔日历日期↔日报/提示词路径」对照表，并在 `prompts/24320121/D3/mapping_and_export.md` 附索引；此后每天手工按对位路径落文件，D4 当天 20:18 的 `5a83ee1` 已同日对位成功。根因未修：`scripts/submit_tool.py` 自 `0a4d222` 起 `START_DATE=2026-09-02`，今日 12 条提交无一触及该文件，仍待修正。
2. **追问②（SKILL.md 是否实际使用）**：`daily-report` 本次实际用于生成本日报，其规则明文要求非首日必须回答上一日报或系统反馈中的遗留问题，即保证第一节先逐条复述并回答系统追问、再写自选项（本节即为执行）；`prompt-trimmer` 今日未实际调用——D5 提示词一次写入 5828 字即低于 1 万字上限，无需截断。

## 今日目标

- 打通 Flutter web 打包，使 web 采集（getUserMedia+AudioWorklet）在浏览器可用；当日记录按对位路径落库。

## 实际进展

**Flutter web 打包全链路（12 条提交）**
- `7b56699`/`fea0681`/`1c78ab9`：WebMicSource（getUserMedia+AudioWorklet）、web 脚手架、绑定层工厂化；`e29a00c`/`11328e3`/`365f32f`：unsafeCast 移除、请求体、FFI 惰性查找修复；`e096cea`：dart:io HttpClient 换 package:http（web 走 fetch），新增 /voice-debug 调试页。
- 验证（Codespace musical-invention-p49pw6j5g5jcrwvp，Flutter 3.47.2）：flutter analyze 0 error/0 warning；flutter test（VM）72 通过、2 失败（与本模块无关的既有用例）；flutter build web 通过；服务端 go build/`go test ./...` 全绿。
- `ecab68c`/`6e9f715`/`25bad9d`/`1224fec`/`cf9a7ac`：README 验证与三段式备忘、D5 提示词导出（5828 字）。今日无个人 Issue/PR。

## 遇到的问题与解决

1. **web 上 dart:ffi 不存在**：原生 FFI 绑定构造即抛错。改为惰性 `late final` 符号查找 + 平台工厂 `createDefault*Bindings()`，以 web 编译图覆盖验证。
2. **下游引用分支专属导出名编译失败**：NativeMicSource/NativeAudioSink 改用两分支同名导出的平台工厂。

## 明日计划

- 修根因：`submit_tool.py` 的 START_DATE 改为按课程日历推导（D1=2026-09-01）并单独提交，让双路径映射退役；清理 `prompts/24320121/D4/.gitkeep`。
- R8.8 弱网联调：注入延迟/丢包，记录 60ms 滞后起播阈值依据，使真机延迟数据可回溯到对话与提交。
