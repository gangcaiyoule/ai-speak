# D6 日报

## 昨日遗留问题的回答

1. submit_tool.py check 适配：今日未执行，仅影响本地体检，顺延至归档日。
2. R8.8 弱网与真机回环延迟：未协调到真机未执行；60ms 阈值实测依据继续挂起，已在设计报告如实标注。

## 今日目标

打通 web 演示链路，完成期末报告初稿。

## 实际进展

- `9ff4db0`：WirePracticeClient 迁移 `package:http`，补齐 D5 遗漏的第三个 wire 客户端，`flutter build web` 通过。
- `de3df49`：服务端 `main.go` 静态托管 web 打包产物，浏览器同源访问，消除跨源授权限制。
- 验证（如实记录）：今日仅实际执行 `flutter build web`；`flutter analyze`/`flutter test` 与 `go build`/`go test ./...` 未在容器重跑，`main.go` 的 Go 侧验证待补。
- 完成 `docs/24320121/设计报告.md`、`思政报告.md`；课程反馈本人独立撰写。
- 期末答辩问答指出其他组越权访问漏洞（可横向读取他人数据），并说明本项目按用户 ID 隔离资源的接口设计。
- 今日无个人 Issue/PR，提交直接落 `dev/voice_stream`。

## 遇到的问题与解决

1. web 打包再次失败：D5 只迁移了 identity、scene 客户端，WirePracticeClient 仍引用 `dart:io`。逐条编译错误排查补齐，把「wire 客户端全平台无关」记为检查项。
2. 打包产物跨源被浏览器拦截：改为 Go 服务端同源托管 web 目录。

## 明日计划

补跑 `go build`/`go test ./...`；浏览器实测 `/voice-debug` 采集；适配 `submit_tool.py check`；完成期末归档提交。
